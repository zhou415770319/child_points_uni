# 儿童积分任务生成系统 - 完整方案文档

## 📋 目录

1. [系统概述](#系统概述)
2. [功能模块设计](#功能模块设计)
3. [技术架构设计](#技术架构设计)
4. [数据库设计](#数据库设计)
5. [API 接口设计](#api接口设计)
6. [前端页面设计](#前端页面设计)
7. [部署方案](#部署方案)
8. [安全设计](#安全设计)

---

## 1. 系统概述

### 1.1 项目背景

儿童积分任务生成系统是一款面向家庭的学习激励平台，通过积分机制激励儿童完成学习任务，培养良好的学习习惯。

### 1.2 核心价值

- **激励学习**：通过积分奖励机制激发儿童学习动力
- **亲子互动**：家长可参与任务创建、审核和奖励发放
- **数据追踪**：记录学习进度，生成可视化报告

### 1.3 双角色系统

| 角色 | 功能定位 | 核心权限 |
|------|---------|---------|
| **家长** | 任务管理者 | 创建任务、审核打卡、查看报告 |
| **儿童** | 任务执行者 | 完成任务、获取积分、兑换礼品 |

---

## 2. 功能模块设计

### 2.1 用户认证模块

- 用户注册（支持家长/儿童角色）
- 用户登录（支持角色选择）
- 家长二级密码验证
- 密码找回/重置

### 2.2 儿童管理模块

- 儿童信息录入（姓名、年龄、年级、爱好）
- 儿童信息编辑
- 儿童关联管理
- 学习测试（初次进入时进行）

### 2.3 任务管理模块

- 任务模板创建（内置模板如 337 阅读法）
- AI 一键生成任务
- 任务难度调整
- 自定义任务创建

### 2.4 打卡模块

- 任务打卡
- 作业上传与 AI 批改
- 智能评价与建议
- 巩固练习题生成

### 2.5 积分管理模块

- 积分获取（普通积分 + 奖励积分）
- 积分消费（礼品兑换）
- 积分记录查询
- 积分规则配置

### 2.6 礼品商城模块

- 礼品展示与分类
- 礼品兑换
- 库存管理
- 兑换记录

### 2.7 学习报告模块

- 学习数据分析
- 进度可视化图表
- 周报/月报生成
- 目标设定与追踪

### 2.8 教材管理模块

- 教材添加与管理
- 习题册配置
- 任务自动生成

---

## 3. 技术架构设计

### 3.1 整体架构

```
┌─────────────────────────────────────────────────────────────────┐
│                        前端 (React)                            │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐          │
│  │ 家长端   │ │ 儿童端   │ │ 登录页   │ │ 公共组件 │          │
│  └────┬─────┘ └────┬─────┘ └────┬─────┘ └──────────┘          │
└───────┼────────────┼────────────┼──────────────────────────────┘
        │            │            │
        └────────────┼────────────┘
                     ▼
┌─────────────────────────────────────────────────────────────────┐
│                        API 网关                                │
│                    Nginx / CORS 处理                           │
└────────────────────────────┬────────────────────────────────────┘
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                        后端 (Python FastAPI)                    │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐          │
│  │ 用户认证 │ │ 儿童管理 │ │ 任务管理 │ │ 积分系统 │          │
│  └────┬─────┘ └────┬─────┘ └────┬─────┘ └────┬─────┘          │
│       │            │            │            │                  │
└───────┴────────────┴────────────┴────────────┴─────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                      数据库 (PostgreSQL)                        │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐          │
│  │ users    │ │ children │ │ tasks    │ │ points   │          │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘          │
└─────────────────────────────────────────────────────────────────┘
```

### 3.2 技术栈

| 层级 | 技术 | 版本 | 说明 |
|------|------|------|------|
| 前端框架 | React | 18.x | 前端 UI 框架 |
| 构建工具 | Vite | 6.x | 快速构建工具 |
| 样式框架 | TailwindCSS | 3.x | CSS 框架 |
| UI 组件 | Ant Design | 5.x | 组件库 |
| 状态管理 | Zustand | 4.x | 轻量级状态管理 |
| 路由 | React Router | 6.x | 路由管理 |
| 图表 | ECharts | 5.x | 数据可视化 |
| 后端框架 | FastAPI | 0.104.x | Python Web 框架 |
| 数据库 | PostgreSQL | 15.x | 关系型数据库 |
| ORM | SQLAlchemy | 2.x | 数据库 ORM |
| 密码加密 | bcrypt | 4.x | 安全密码哈希 |
| JWT | PyJWT | 2.x | 身份认证 |

### 3.3 项目结构

```
childer_manager/
├── frontend/                    # 前端项目
│   ├── src/
│   │   ├── pages/              # 页面组件
│   │   │   ├── Login.jsx      # 登录页
│   │   │   ├── Parent/        # 家长端页面
│   │   │   └── Child/         # 儿童端页面
│   │   ├── services/          # API 服务
│   │   ├── store/             # 状态管理
│   │   ├── components/        # 公共组件
│   │   └── utils/             # 工具函数
│   ├── .env                   # 环境变量
│   └── package.json           # 依赖配置
│
├── backend/                    # 后端项目
│   ├── app/                   # 应用代码
│   │   ├── api/               # API 路由
│   │   ├── models/            # 数据库模型
│   │   ├── schemas/           # Pydantic 模式
│   │   ├── services/          # 业务逻辑
│   │   └── utils/             # 工具函数
│   ├── .env                   # 环境变量
│   ├── requirements.txt       # 依赖列表
│   └── main.py               # 入口文件
│
└── docker-compose.yml         # Docker 部署配置
```

---

## 4. 数据库设计

### 4.1 用户表 (users)

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | INTEGER | PRIMARY KEY AUTOINCREMENT | 用户ID |
| phone | VARCHAR(20) | UNIQUE NOT NULL | 手机号 |
| password | VARCHAR(255) | NOT NULL | 密码哈希 |
| name | VARCHAR(50) | NOT NULL | 姓名 |
| role | VARCHAR(10) | NOT NULL | 角色 (parent/child) |
| second_password | VARCHAR(255) | NULL | 家长二级密码 |
| createdTime | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| updatedTime | DATETIME | DEFAULT CURRENT_TIMESTAMP ON UPDATE | 更新时间 |

### 4.2 儿童表 (children)

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | INTEGER | PRIMARY KEY AUTOINCREMENT | 儿童ID |
| parent_id | INTEGER | FOREIGN KEY → users.id | 关联家长ID |
| name | VARCHAR(50) | NOT NULL | 儿童姓名 |
| age | INTEGER | NOT NULL | 年龄 |
| grade | VARCHAR(20) | NOT NULL | 年级 |
| hobby | TEXT | NULL | 爱好（逗号分隔） |
| avatar | VARCHAR(255) | NULL | 头像URL |
| total_points | INTEGER | DEFAULT 0 | 总积分 |
| test_completed | BOOLEAN | DEFAULT FALSE | 是否完成测试 |
| test_score | INTEGER | NULL | 测试分数 |
| created_at | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

### 4.3 任务表 (tasks)

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | INTEGER | PRIMARY KEY AUTOINCREMENT | 任务ID |
| parent_id | INTEGER | FOREIGN KEY → users.id | 创建者ID |
| child_id | INTEGER | FOREIGN KEY → children.id | 关联儿童ID |
| title | VARCHAR(100) | NOT NULL | 任务标题 |
| description | TEXT | NULL | 任务描述 |
| type | VARCHAR(20) | NOT NULL | 任务类型 |
| difficulty | VARCHAR(10) | NOT NULL | 难度 (easy/medium/hard) |
| base_points | INTEGER | NOT NULL | 基础积分 |
| deadline | DATETIME | NULL | 截止时间 |
| status | VARCHAR(10) | DEFAULT 'active' | 状态 |
| created_time | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| updated_time | DATETIME | DEFAULT CURRENT_TIMESTAMP ON UPDATE | 更新时间 |

### 4.4 打卡记录表 (checkins)

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | INTEGER | PRIMARY KEY AUTOINCREMENT | 打卡ID |
| task_id | INTEGER | FOREIGN KEY → tasks.id | 关联任务ID |
| child_id | INTEGER | FOREIGN KEY → children.id | 关联儿童ID |
| content | TEXT | NULL | 打卡内容 |
| ai_score | INTEGER | NULL | AI评分 |
| ai_comment | TEXT | NULL | AI评价 |
| review_status | VARCHAR(10) | DEFAULT 'pending' | 审核状态 |
| review_points | INTEGER | NULL | 审核后积分 |
| checkin_time | DATETIME | DEFAULT CURRENT_TIMESTAMP | 打卡时间 |

### 4.5 积分历史表 (points_history)

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | INTEGER | PRIMARY KEY AUTOINCREMENT | 记录ID |
| child_id | INTEGER | FOREIGN KEY → children.id | 关联儿童ID |
| type | VARCHAR(20) | NOT NULL | 类型 (earn/spend) |
| amount | INTEGER | NOT NULL | 积分数量 |
| description | VARCHAR(200) | NULL | 描述 |
| related_id | INTEGER | NULL | 关联ID |
| created_at | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

### 4.6 礼品表 (gifts)

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | INTEGER | PRIMARY KEY AUTOINCREMENT | 礼品ID |
| name | VARCHAR(100) | NOT NULL | 礼品名称 |
| description | TEXT | NULL | 礼品描述 |
| image_url | VARCHAR(255) | NULL | 图片URL |
| price | INTEGER | NOT NULL | 积分价格 |
| stock | INTEGER | DEFAULT 0 | 库存 |
| category | VARCHAR(50) | NULL | 分类 |
| created_at | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

### 4.7 兑换记录表 (exchanges)

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | INTEGER | PRIMARY KEY AUTOINCREMENT | 兑换ID |
| child_id | INTEGER | FOREIGN KEY → children.id | 关联儿童ID |
| gift_id | INTEGER | FOREIGN KEY → gifts.id | 关联礼品ID |
| status | VARCHAR(10) | DEFAULT 'pending' | 状态 |
| created_at | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

### 4.8 教材表 (textbooks)

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | INTEGER | PRIMARY KEY AUTOINCREMENT | 教材ID |
| child_id | INTEGER | FOREIGN KEY → children.id | 关联儿童ID |
| name | VARCHAR(100) | NOT NULL | 教材名称 |
| total_pages | INTEGER | NOT NULL | 总页数 |
| pages_per_task | DECIMAL(5,2) | DEFAULT 1 | 每次任务页数 |
| status | VARCHAR(10) | DEFAULT 'active' | 状态 |
| subject | VARCHAR(50) | NULL | 科目 |
| created_at | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

---

## 5. API 接口设计

### 5.1 用户认证接口

| 方法 | 路径 | 功能 |
|------|------|------|
| POST | `/api/auth/register` | 用户注册 |
| POST | `/api/auth/login` | 用户登录 |
| POST | `/api/auth/logout` | 用户登出 |
| POST | `/api/auth/verify-second-password` | 验证二级密码 |

#### 注册接口

**请求：**
```json
{
  "phone": "13800138000",
  "password": "123456",
  "name": "张三",
  "role": "parent",
  "second_password": "654321"
}
```

**响应：**
```json
{
  "success": true,
  "data": {
    "id": 1,
    "phone": "13800138000",
    "name": "张三",
    "role": "parent"
  }
}
```

#### 登录接口

**请求：**
```json
{
  "phone": "13800138000",
  "password": "123456",
  "role": "parent",
  "second_password": "654321"
}
```

**响应：**
```json
{
  "success": true,
  "data": {
    "id": 1,
    "name": "张三",
    "role": "parent",
    "token": "jwt-token-here"
  }
}
```

### 5.2 儿童管理接口

| 方法 | 路径 | 功能 |
|------|------|------|
| GET | `/api/children` | 获取儿童列表 |
| POST | `/api/children` | 添加儿童 |
| GET | `/api/children/{id}` | 获取单个儿童 |
| PUT | `/api/children/{id}` | 更新儿童信息 |
| DELETE | `/api/children/{id}` | 删除儿童 |

### 5.3 任务管理接口

| 方法 | 路径 | 功能 |
|------|------|------|
| GET | `/api/tasks` | 获取任务列表 |
| POST | `/api/tasks` | 创建任务 |
| PUT | `/api/tasks/{id}` | 更新任务 |
| DELETE | `/api/tasks/{id}` | 删除任务 |
| POST | `/api/tasks/generate` | AI 生成任务 |

### 5.4 打卡接口

| 方法 | 路径 | 功能 |
|------|------|------|
| GET | `/api/checkins` | 获取打卡记录 |
| POST | `/api/checkins` | 创建打卡 |
| PUT | `/api/checkins/{id}/review` | 审核打卡 |

### 5.5 积分接口

| 方法 | 路径 | 功能 |
|------|------|------|
| GET | `/api/points/balance/{child_id}` | 获取积分余额 |
| GET | `/api/points/history/{child_id}` | 获取积分历史 |

### 5.6 礼品接口

| 方法 | 路径 | 功能 |
|------|------|------|
| GET | `/api/gifts` | 获取礼品列表 |
| POST | `/api/gifts` | 添加礼品 |
| PUT | `/api/gifts/{id}` | 更新礼品 |
| DELETE | `/api/gifts/{id}` | 删除礼品 |
| POST | `/api/exchanges` | 兑换礼品 |

### 5.7 报告接口

| 方法 | 路径 | 功能 |
|------|------|------|
| GET | `/api/reports/daily/{child_id}` | 获取日报 |
| GET | `/api/reports/weekly/{child_id}` | 获取周报 |
| GET | `/api/reports/monthly/{child_id}` | 获取月报 |

---

## 6. 前端页面设计

### 6.1 页面架构

```
路由结构：
/
├── login                    # 登录页（公共）
├── parent/                  # 家长端
│   ├── dashboard            # 仪表盘
│   ├── children             # 儿童管理
│   ├── tasks               # 任务管理
│   ├── checkins            # 打卡审核
│   ├── textbooks           # 教材管理
│   └── reports             # 学习报告
└── child/                  # 儿童端
    ├── home                # 首页（任务列表）
    ├── checkin/{task_id}   # 打卡详情
    ├── mall                # 积分商城
    ├── profile             # 个人中心
    ├── progress            # 学习进度
    └── test                # 学习测试
```

### 6.2 登录页

- 登录/注册切换
- 角色选择（家长/儿童）
- 家长二级密码输入框

### 6.3 家长端仪表盘

- 儿童列表快速选择
- 今日任务统计
- 待审核打卡提醒
- 学习数据概览

### 6.4 儿童端首页

- 今日任务列表
- 积分余额展示
- 任务完成进度
- 快捷打卡入口

---

## 7. 部署方案

### 7.1 开发环境

```bash
# 启动后端
cd backend
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
uvicorn main:app --reload

# 启动前端
cd frontend
npm install
npm run dev
```

### 7.2 Docker 部署

```yaml
version: '3.8'
services:
  db:
    image: postgres:15
    environment:
      POSTGRES_DB: childer_db
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
  
  backend:
    build: ./backend
    environment:
      DATABASE_URL: postgresql://admin:password@db/childer_db
      JWT_SECRET: your-secret-key
    ports:
      - "8000:8000"
    depends_on:
      - db
  
  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend

volumes:
  postgres_data:
```

### 7.3 环境变量配置

**后端 .env:**
```env
DATABASE_URL=postgresql://admin:password@localhost/childer_db
JWT_SECRET=your-jwt-secret-key
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=1440
```

**前端 .env:**
```env
VITE_API_BASE_URL=http://localhost:8000/api
VITE_DEBUG=true
```

---

## 8. 安全设计

### 8.1 密码安全

- 使用 bcrypt 进行密码哈希
- 密码最小长度 8 位
- 支持特殊字符

### 8.2 JWT 认证

- 使用 JWT 进行无状态认证
- Token 有效期 24 小时
- 支持 Token 刷新机制

### 8.3 权限控制

- 基于角色的访问控制
- 家长操作需要二级密码验证
- API 接口权限校验

### 8.4 数据安全

- 敏感数据加密存储
- API 请求频率限制
- SQL 注入防护（使用 ORM）

---

## 📅 开发计划

| 阶段 | 时间 | 任务 |
|------|------|------|
| 第一阶段 | 第1-2周 | 后端基础框架 + 用户认证 |
| 第二阶段 | 第3-4周 | 儿童管理 + 任务系统 |
| 第三阶段 | 第5-6周 | 打卡模块 + 积分系统 |
| 第四阶段 | 第7-8周 | 前端页面开发 |
| 第五阶段 | 第9-10周 | 测试与部署 |

---

**文档版本**: v2.0  
**创建日期**: 2026年5月  
**适用项目**: 儿童积分任务生成系统